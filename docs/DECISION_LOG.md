# Decision Log

設計判断の記録。なぜその選択をしたかを残す。

Phase 0 着手時点の判断は、Agent Bridge AB-63 〜 AB-81 の往復で確定したもの。両環境（Leon / NiKo）の実測に基づく。

---

## 2026-08-19 — Phase 0 着手前に確定した 6 件

### 決定 1: 時系列を第1弾にし、母集団比較を第2弾に回す

**当初案**: 母集団順位が第1弾、時系列は後。

**採用理由**:
- **時系列はゲーム化に強い**。単調増加／単調減少の軸は例外なく破られる（トークン量は巨大ファイルを繰り返し読むだけ、手戻り率は Edit をやめて Write に寄せるだけ、エラー率は `command || true` の数文字で破れる）。耐えるのは比率と時間微分の 2 つだけ
- **N=1 で成立する**。母集団を待たずに製品が動く
- **層別なしの percentile は情報を持たない**。エージェント環境は用途の分散が桁違いに大きい
- 「エージェントが賢くなり続ける」という製品目的に、時系列のほうが直結している

**却下した論拠**: 「順位がないと誰も他人に見せない」。同一ニッチの自然実験が真逆を示した — ccusage（順位ゼロの個人用集計 CLI）が GitHub 18,000 star に対し、その出力を提出する順位板 viberank は 114 star。Spotify Wrapped は順位ゼロで 2023 年版 20 億インプレッション。WakaTime は順位を 16 ヶ月後に足している。

### 決定 2: 提出単位はマシン全体。プロジェクト別内訳を全件入れ、閾値は設けない

**当初案**: 「きれいなプロジェクトだけ提出する」を防ぐため、加重平均の閾値 M を設ける。

**却下理由**:
- **n=2 の両方が単一プロジェクト支配**（Leon 100% / NiKo 99.6%）。この 2 件から決めた M は、分布が違う環境で破綻する式を「実測で決めた」と称することになる
- n=2 で仮置きした閾値は、後で変えても既提出分を再分類できない
- 部分提出を受け付けないだけで防げる。閾値判定のロジックが不要になる分、実装はむしろ単純

**副次的な根拠**: Leon 環境の `usage_log.csv` には 5 アプリの行が残っているのに、生ログを持つプロジェクトは 1 つだけだった。**今見えているプロジェクト分布は実態ではなく、直近の作業履歴の影**。提出単位をマシン全体に置く理由になる。

### 決定 3: 率を送らず、分子と分母を生カウントで送る

**理由**: 率を送った時点で定義が埋め込まれ、受け取り側で揃え直せない。実測で is_error 率は走査範囲だけで 1.26〜1.46 倍、分母定義で 1.96 倍動いた。生カウントなら定義が変わっても再計算できる。

各指標は `numerator` / `denominator` / `denominatorMeaning` の 3 つ組で持つ。`denominatorMeaning` が無いペイロードは拒否する。

### 決定 4: 走査マニフェストを必須項目にする

**背景**: Leon が「`isSidechain` は全行 false」と報告したが、非再帰 glob が `subagents/` を開いていなかっただけだった。同じバグが is_error 率を 1.46 倍動かしていた。3 日間、両者とも気づかなかった。

**問題の質**: データは両環境とも正しく取れている。**壊れるのは比較だけ**。Leon 6.75%（subagents 除く）と NiKo 2.85%（範囲不明）を同じ母集団に入れると、誰も気づかないまま無意味な順位が出る。

**必須項目**: 走査パス / 再帰の有無 / ファイル数 / 行数 / Claude Code バージョン分布 / `originFieldCoverage` / `activeDaysMethod` / `measuredAt` / `sourceField`。

- `measuredAt` — 同じ環境でも数時間で値が動く（実測でファイル数 156 → 167）。時刻が無いと同一環境の冗長提出を区別できない
- `sourceField` — 同じ指標を別フィールドから取るケースが実際に出た（決定 6）。由来を持たないと、将来 `preventedContinuation` が生き返ったときに旧提出分と混ざる

### 決定 5: `activeDays` は観測された日の和集合で数える

**却下した定義**: `max(git のコミット日数, jsonl の日付ユニーク数)`。

**却下理由**: **Leon 環境で 107.1% を返した**。CSV に `2026-04-17/18/19` の 3 日があるが、初コミットは 04-20 なので git に存在せず、当時の jsonl は刈られている。分母が分子を下回りうる定義だった。

**採用**:
```
activeDays = |union(git のコミット日, jsonl の日付, 外部ログの日付)|
```
分子（外部ログの日付）が分母の部分集合になるので、構造的に 100% を超えない。意味も明確になる — 「作業した証拠のある日のうち、外部ログに入っている割合」。

**限界を明記する**: これは真の稼働日の**下限**でしかない。コミットもログも残さずに終わった日は原理的に見えない。だから `activeDaysMethod: "union-of-observed"` をマニフェストに入れ、将来定義を変えても層別できるようにする。

**併せて暦日数分母の値も持つ**（`recordRateCalendar`）。これは記録率ではなく作業頻度を答える別の指標だが、捨てると「なぜ実日数が少ないのか」が読めなくなる。**2 つの分母を両方持ち、それぞれが何を答えているかをラベルで区別する。**

### 決定 6: 軸6（書いたルール vs 守られたルール）は jsonl 層を必須、外部ログ層を任意にする

**当初案**: データ源は外部 hook ログ。無ければ `not_applicable`。

**却下理由**: 外部 hook ログを持つのは自分で仕込んだ環境だけ。NiKo は `logs/hook_events.jsonl` に 563,613 行あるが、**Leon は 1 行も持たない**（`logs/` は空ディレクトリ、hook 7 本とも書き出しゼロ）。これでは 2 環境中 1 環境が `not_applicable` になり、中心軸が半分の環境で測れない。

**発見**: jsonl から取れた。`preventedContinuation` は 278 件すべて false だが、**`hookErrors` 非空が 9 件（3.2%）**あり、中身は差し戻し文そのもの。9 件全部が `check_response_quality.py` の「質問は一つずつ」ルール違反だった。

**採用**:
- 分子 = `hookErrors` 非空件数、分母 = `stop_hook_summary` 総数。**両方 jsonl にあるので全環境で比率が出せる**
- 外部ログを持つ環境は、さらに細かい分解（warned / triggered の区別）ができるので**上乗せの任意層**に回す

---

## 横断する教訓 — なぜバリデーションを受け取り側に置くのか

設計期間 2 日で定義依存の事故が 4 件起きた。**うち 2 件は「定義を明示せよ」というルールを書いた本人が直後に破ったもの。**

- NiKo はルール制定の数時間後に、Leon の数字を検証せず設計書に取り込んだ
- Leon は「パーサが静かに 0 を返す」と自分で警告した直後に、非再帰 glob をやった

**送る側の規律では止まらないことが 4 回証明された。** だから受け取り側で拒否する。マニフェストの無いペイロードは受け付けない。

そしてこの事実自体が製品の芯になっている:

> **単一環境で自己採点するツールは、自分のパーサのバグを永遠に検出できない。**

三値フラグも走査マニフェストも、結局は「外部から検算できる形で出せ」という同じ要請の別名だった。この 2 日で、Leon は非再帰 glob を、NiKo は推定値の混入を見つけた。どちらも自分では見つけられなかった。

---

## テストの方針

**拒否条件は、拒否できることを先に示してから通す。**

Leon 環境で `usage_collector.py` の rebuild バグを直したとき、`starting_rows` を `return {}` に戻してテストが落ちること（exit 1）を確認してから修正版を通した。同じ形を V-1〜V-8 の各条件に適用する。

**フィクスチャは実際に起きた事故そのものを使う**:

| 実際の事故 | 拒否/警告すべき形 |
|---|---|
| 非再帰 glob | `scope: "all"` なのに `subLineRatio: 0` |
| is_error の分母未明示 | 率だけあって `numerator` / `denominator` 無し |
| human 定義の乖離 | `originFieldCoverage: 0.034` |
| 記録率 107% | `recordRate.numerator > denominator` |
| ブール値が死んでいる | `sourceField` 無し |

実際の事故を固定しておけば、同じ穴をリグレッションとして二度踏まない。今回の 4 件は全て「検出器が動くことを確かめなかった」形だった。

---

## 2026-08-20 — 仕様書2本の食い違い3件（S7a で確定）

S7 の実装に入る前に窓の定義を読み直したところ、`agent_eval_axes_v2.md` と `submission_payload_spec_v1.md` が3箇所で食い違っていた。3件とも**受信側が黙って比較に失敗する**形で、例外にはならない。

### 1. `windowSource` の第2値: `observed` か `measured` か

| 出典 | 値 |
|---|---|
| `submission_payload_spec_v1.md` §2 | `"setting" \| "observed"` |
| `agent_eval_axes_v2.md`（2箇所） | `"setting" \| "measured"` |

同じものを指す2つの語。閉じた union の照合は文字列一致で行うので、送信側が `observed`・受信側が `measured` なら**どの環境も一致せず、片方が黙って落ちる**。

**採用: `observed`。** ペイロードの語彙はペイロード仕様が決める。ただし受信側が軸仕様から実装されていれば1行の差なので、AB-87 で確認に出した。

### 2. `activeDays` が同一環境で2つの値を持つ

| パス | 値 | 意味 |
|---|---|---|
| `window.activeDays` | **17** | 人間発話が1件以上ある日。全軸の分母スコープ |
| `externalLog.activeDays` | **18** | 何らかの証拠がある日の和集合。記録率の分母 |

どちらも正しく、答えている問いが違う。危険なのは**親オブジェクトを見ずに `activeDays` を読むと 5.9% ずれる**こと。事故#1〜#4 と同じ形。

**対策:** 型を分けた（`WindowDaysMethod` = `'human-turn-days'` / `ActiveDaysMethod` = union 系）。ラベルを取り違えた値は構築できない。加えて 2 つの検算ルールを追加:

- **V-13** — `externalLog.recordRate.denominator` は `externalLog.activeDays` と一致すること。分数の中にしか存在しない分母は、何とも突き合わせられない
- **V-14** — `window.activeDays ≤ externalLog.activeDays`。人間発話があった日はその発話を読んだ jsonl を残しているので、和集合に必ず含まれる。**大小逆転は構造的に不可能**＝入れ替えの検出器になる

### 3. base 自身が矛盾していた（main 上の実バグ）

`windowSource: 'setting'` と `cleanupPeriodDays: null` が同居していた。「設定から読んだ」と言いながら「何を読んだかは無い」状態。このマシンには4スコープのどこにもキーが無いので、正しくは `observed`。

**V-15** を追加した。`setting` なら period 必須、`observed` なら period は null。

### 陽性対照

| 対照 | 結果 |
|---|---|
| V-13/14/15 を `add()` で握りつぶす | 該当の拒否テスト4件だけが失敗、他 111 件は無傷 |
| base を修正前の値（18 / `setting`）に戻す | base 拒否 + W 系 fixture が全滅（12件） |

1 回目の対照は perl の置換が一致せず**対照が走らないまま 115 件通過**した。`grep -c` で「対照が入っていること」を先に確認する手順に変えた。検出器を無効化したつもりで無効化できていない場合、出力は「全部通る」で、成功と見分けがつかない。

---

## 2026-08-20 — S7b: 窓と外部ログの生成側

### 実機で出た数字（Python 独立実装と照合済み）

```
人間発話日 (origin.kind=='human')  =  5   2026-08-16 〜 08-20
user 行のある日                    =  9   ↑ + 2026-07-08〜07-11
jsonl の日                         =  9
git コミット日                     = 15   2026-04-20 〜 08-20（2リポ）
外部ログ (usage_log.csv)           = 42 行 / 19 日
証拠日 = union(git, jsonl, 外部)   = 23
記録率                             = 19/23 = 82.6%
cleanupPeriodDays                  = null（4スコープ全てに不在）
```

人間発話日・user行日・jsonl日・git日・cleanupPeriodDays の5項目は **TS 実装と Python 実装で完全一致**。

### 発見1: 最初のゲートが origin カバレッジで決まっている

仕様は `activeDays < 5` で環境まるごと `not_applicable`（総合点を出さない）。このマシンの実測は **ちょうど 5**。

そして `activeDays` = 人間発話日は `origin` フィールドを持つ行からしか数えられず、**このマシンの origin カバレッジは 3.11%**。7月の4日間は user 行があるのに origin が1件も無いため、稼働日として数えられていない。

つまり **1日ずれれば環境が採点対象から外れ、どちら側に落ちるかは「どのバージョンがログを書いたか」で決まる**。作業量ではない。

**対策:** `window.userRowDays`（user 行のある日 = 9）を併記する。閾値を発明せずに、受信側が感度を自分で計算できる。加えて **V-16**（人間発話日 ⊆ user行日 ⊆ 証拠日）で3値の入れ子を固定。

### 発見2: `externalOnlyDays` が構造的に常に 0 だった

「外部ログにしか無い日」を**分母集合と比較**して数えていた。ところが分母は外部ログを和集合に含んでいるので、**実機では必ず 0**。

これは `isSidechain` / `toolUseResult.interrupted` / `preventedContinuation` と同じ形 — 定数ゼロを返し続けるフィールドは、動いている検出器と見分けがつかない。

**修正:** 生ソース（jsonl ∪ git）と比較する。実機で **7 日**。107% を生んだのはまさにこの種の日。

### 発見3: 記録率が 1 を超えられない理由は2つあり、どちらか片方では検証にならない

- 外部ログの日を分母（和集合）に入れている
- 分子を分母集合との**交差**で作っている

`numerator <= denominator` だけを assert すると、**どちらを外しても通る**。実際、最初に書いたテストは両方を外さないと落ちなかった＝何も検証していなかった。

各機構が固有に生む値で固定し直した:

| 対照 | 落ちるテスト | 症状 |
|---|---|---|
| 和集合から外部ログを外す（= max 相当） | 「log-only day を分母に入れる」 | `evidenceDays` 6 → 3 |
| 交差をやめて外部ログ日数をそのまま分子に | 「分母が持つ日だけ数える」 | `makeMetric` が「分子4 > 分母3」で拒否 |

### git に聞くのはコミット日だけ

`GIT_ARGS = ['log', '--format=%cd', '--date=short']` を定数にし、argv 自体をテストで固定した。git クライアントが入る最初の場所は、PR 数・マージ帰属・revert 率を足すのが最も安い誘惑になる。v1 §15 はそれらを reject 済み（`pr-link` は Leon 284 / NiKo 0 で、ホストの差を測ってしまう）。

### プロジェクト名からリポジトリパスは復元しない

`C--Users-katuy-Yama-Agent` は `C:\Users\katuy\Yama-Agent` にも `C:\Users\katuy\Yama\Agent` にも復号でき、名前の中に区別材料が無い。デコーダは大半で当たり、外れたときは**「コミットの無いリポジトリ」に見える** — 静かなゼロ。git 日はリポジトリを明示的に渡されたときだけ数え、`activeDaysMethod` がそれを言う。

### 行数のドリフト

Python 29,652 → TS 29,892 → TS 29,922 と単調増加した。進行中セッションが自分のトランスクリプトに書き足しているため。**同一環境の2回の提出を区別するにはマニフェストに `measuredAt` が要る**ことの実データ。

---

## 2026-08-20 — S11a: 環境収集

### 実機で出た数字（Python 独立実装と全項目一致）

```
permissions  allow=67  deny=3  ask=0            （user 16 + project 0 + local 51）
  分類       unrestrictedExec=0  cliWildcard=27  scriptPathFixed=24  exact=16
skills       .claude/skills/*/SKILL.md =  0
             .claude/skills/*.md       = 27      union = 27
hooks        5 件 / 3 イベント（PreToolUse / SessionStart / Stop）
mcpServers   1 件（~/.claude.json の top-level）
```

### 発見: MCP サーバー定義は settings 4スコープに無い

`settings*.json` の4スコープは**全て 0**。実体は `~/.claude.json` の top-level `mcpServers`。

これは AB-87 で NiKo に投げた Q-11（`mcpUsed` の分母ソース）の実測回答。settings スイープだけで作った収集器は **`mcpUsed` の分母を 0 にする** — V-4 は通り、W-6 の下限 100 未満、結果 0/0 の穴。

読む先を `.mcp.json` / `~/.claude.json` / `~/.claude/settings.json` の union にした。

### permissions は「マージ」であって「上書き」ではない

Claude Code は全スコープのリストを適用するので、実効面は和集合。最高優先スコープだけを見ると **このマシンで 67 件を 16 件と報告する**。

### 文字列は載せない — 実データで確認した理由

このマシンの allow に含まれるもの:

```
node scripts/emit-service-key.mjs:*
node scripts/ban-test-accounts.mjs:*
node ../anki-new/scripts/emit-service-key.mjs:*
```

スクリプト名が「このマシンが何をして良いか」を説明し、相対パスが**別プロジェクト名**を運ぶ。仕様のスケッチ（`"effectivelyUnrestrictedPatterns": ["Bash(python:*)"]` を文字列で送る）は、同じ問いに `{class, count}` で答えられる。

### 分類器は allow だけに掛ける

`deny` のワイルドカードはリスクの逆。同じに数えると、**丁寧に締めた環境ほど開いて見える**。

### 3つのゼロに全て陽性対照を付けた

このマシンで 0 を返す3項目は、壊れた収集器の出力と同じ:

| 0 になる項目 | 対照 | 結果 |
|---|---|---|
| `unrestrictedExec` | 実データに `Bash(python:*)` を1件足す | 0 → 1 |
| `skills/*/SKILL.md` | 入れ子レイアウトの glob 結果を注入 | 0 → 1 |
| settings 内の MCP | `~/.claude.json` を読む経路 | 0 → 1 |

`unrestrictedExec` の対照は**単体テストではなく実データ経路**で取った。テストの偽データで通る分類器が、実ファイルの形では動かないことがあるため。

### 3クラスは allow の分割ではない

`unrestrictedExec + cliWildcard + scriptPathFixed + exact = allow` だが、`exact`（ワイルドカード無し）はリスククラスではない。3クラスはリスクの部分集合。

---

## 2026-08-20 — S8 + 組み立て + `scan`: 初めて通しで動いた

`agent-eval scan` が実データからペイロードを作り、**自分の検証器を通す**ところまで到達。

```
scanned    249 files, 30,723 lines, 0 unparsed
           main 18,377 / sub 12,346 (share 0.4018)
projects   5      versions 5      origin 288/8,874
window     人間発話日 5 / user行日 9 / observed
evidence   23 日 (union-of-observed(git, jsonl, externalLog))
record     19/23
gate       passed
axes       0/11 available   （11 × too-few-clusters, 9 × definition-pending,
                              1 × first-window, 1 × no-external-log）
validation passed (0 violations, 1 flags: W-1 origin 3.2%)
```

出力は **11,334 バイト**。`katuy` / `Yama-Agent` / `anki-new` / `emit-service-key` / ドライブレター、いずれも **0 件**。

### 点は1つも出していない

率→点の変換式は v1 軸文書にあり、このリポジトリに無い。埋めるために式を作れば、**誰も擁護できない数字を画面に出す**ことになる。全軸 `score: null`。

代わりに `unavailableReasons` を持たせ、**2種類の理由を分けた**:

- `too-few-clusters` — 実測（11 クラスタ < 最小 20）。仕様が与えた閾値に対する測定結果
- `definition-pending` — この環境の話ではない。`taskBundles` / `SA` / `P1` が手元に無いだけ

素の `not_applicable` だけだと、**実装していない軸について「あなたの環境は小さい」と言う**ことになる。三値 enum に4つ目を足すことは V-7 が禁じているので、区別は理由フィールドに置いた。

### 発見1: `Math.max(1, 分母)` で 3/0 を 3/1 と報告していた

`makeMetric` のゼロ分母拒否を回避するために書いた1行。初回の通し実行が **`skillFired: 3/1`** を出して発覚した — スキルが1つも無いディレクトリで「3種類が27種類中…ではなく1種類中発火」。

**下限 1 は不在を回避しない。不在を「あり得る最小の存在」に改名し、率を分子の倍率だけ跳ね上げる。**

7つの率すべてを `Metric | null` にした。`null` は「この環境にこの分母は無い」。`0/0` は測定されたゼロに読め、下限 1 は 300% に読める。

### 発見2: 測っていない不足を11回報告していた

`axisReasons` が最小分母チェックに `denominator: 0, numerator: 0` を渡していたため、**どの軸も分母を計算していないのに `denominator-below-minimum` を全11軸で報告**していた。

クラスタ項だけを見るよう修正。実測していない項目については `definition-pending` が真実。

### 発見3: `mcpUsed` が 3/1 だった → claude.ai コネクタ

設定ファイル由来の分母が 1、ログ由来の分子が 3。107% と同じ「分子と分母が別の宇宙から来ている」形で、`makeMetric` が拒否して発覚。

「ローカルには列挙できない」と結論する前に `~/.claude.json` を実際に見たところ、**`claudeAiMcpEverConnected` に 9 件**あった。合わせて分母 10、3/10 = 30%。

キー名の `Ever` は効いている。分母は「今つながっているサーバー」ではなく「これまで使えたサーバー」で、`denominatorMeaning` がそれを言う必要がある。

### 自分の出力を自分のルールで検証する

`run()` は payload を **JSON ラウンドトリップしてから** `validate()` に通し、落ちたら **exit 1** で返す。受信側に適用するのと同じルールを送信側にも適用する。

送信側が自分のチェックを免除するのが、DECISION_LOG のインシデント全件の共通形だった。

---

## 2026-08-20 — S6b-a: P1（人間発話の切り出し）

NiKo が v1 軸文書を push（`fd158dc`）。**その P1 定義が、こちらの実装を直接否定していた。**

### 差は 1.65 倍

| | v1 の P1 | 旧実装（`origin.kind` のみ） | 比 |
|---|---|---|---|
| 人間発話 | **413** | 250 | 1.65× |
| 人間発話日 | **9** | 5 | 1.8× |

v1 の記述:

> `origin.kind == "human"` は、実測で text メッセージ144件中134件でキー自体が欠落していた。あれば使う補助条件に留め、**「無い＝人間でない」とは読まない**。

このマシンの origin カバレッジは **3.3%**。つまり 96.7% の user 行を「人間でない」と読んでいた。

### ゲートへの影響が本体

`activeDays` は窓の定義であり、環境ゲートは 5 未満で総合点を止める。

- 旧実装: **5**（ゲートまで**残り1日**）
- P1: **9**（残り4日）

「1日ずれれば環境ごと採点外」と S7b で報告した所見は、**こちらの定義バグが作っていた**。実際には4日の余裕がある。

### P1 の実装

`type == "user"` から次を除外:

| 除外 | 実測（main のみ 4,379 行中） |
|---|---|
| `tool_result` ブロックを含む | 3,959 |
| 先頭300字にチャネルマーカー | 3 |
| 先頭200字が `Base directory for this skill:` | 1 |
| `[Request interrupted by user]` | 3 |
| サブエージェント配下 | 別走査 |
| **残る＝人間発話** | **413** |

除外理由を `notHumanCounts` として件数で持つ。**入力の 90% を落とすフィルタが生存分しか報告しないなら、壊れたフィルタと見分けがつかない。**

### `isSidechain != true` はパスで実装した

v1 は条件を `isSidechain != true` と書いている。両環境でフィールドとパスは一致するが、**main ファイルの行は sidechain であっても false を返す**（main 0件 true / subagents 6,070件 true）。パス判定のほうが静かに壊れない。

### マーカー走査を先頭300字に限る理由

v1 が 300 字と書いているが、根拠はこちらでも成立する。**ツールについて書いている人はこれらを引用する。** 全文走査すると、測定対象そのものを話題にする人だけを黙らせることになる。

境界は両側から到達可能にしてテストした（301字目のマーカーは通す / 300字目のマーカーは落とす）。後者が無いと、前者は「長いテキストは通る」しか証明しない。

### `origin.kind` は補助信号として残す

`originHumanRows` として別に持つ。`humanTurns` との差が、origin カバレッジの代償そのもの。

---

## 2026-08-20 — S6b-b: P5（タスク束）と P3（環境ノイズ）

### 実機（Python 独立測定と一致）

```
humanTurns           = 417   （P1）
taskBundles          = 417   （人間発話1件につき1束）
rootBundles          =  13   （parentUuid = null。Python 実測も 13）
orphanBundles        =   0   （Python 実測も 0）
environmentNoiseRows =  12
除外内訳: tool-result 3,994 / subagent 4,506 / channel-marker 3 / interrupted 4 / skill-injection 1
```

### 順序をチェーンとして読むのは誤り — 実測 4.19%

「ファイル内の行順 = `parentUuid` チェーン」と仮定したくなるが、**実測では 13,421 行中 563 行（4.19%）の親が直前の行ではない**。1セッション（3日にまたがる 6b62be59）は 3,350 行中 304 回分岐していた。

順序で読むと、その 563 行が全部違う束に入る。

### 1パスで済む理由も実測した

非 null の `parentUuid` **13,413 件すべて**が、同一ファイル内の**先行行**を指していた（100%）。よって `uuid → 束` の写像を埋めながら1パスで正しく辿れる。セッションをメモリに保持する必要もない。

### orphan 数が検出器になった

親不明が 0 件であることを**先に実測していた**ので、`orphanBundles` が 0 でなければ実装が誤りだと分かる。これで2つのバグが出た:

| 症状 | 原因 |
|---|---|
| orphan **5,202** | `uuid` を持たないメタ行（`last-prompt` / `ai-title` / `queue-operation` / `pr-link` / `mode` / `file-history-snapshot` 等 5,182 行）に束を割り当てていた |
| orphan **7** | P3 のノイズ行を**木からも外していた**ため、その子が親を見失った |

2件目が要点。**P3 は「分子分母から外せ」であって「木から外せ」ではない。** ノイズ行も束には入れ、カウントだけしない形に直した。

### root と orphan は別の事実

`parentUuid = null` はチェーンの起点（13件）。**「トラッカーが親を見失った」証拠ではない。** 同じ数に混ぜると、正常な起点が実装バグとして報告される。

### orphan の陽性対照

実データで 0 を主張する以上、**orphan が到達可能であること**をテストで示した。到達不能なら、0 はカウンタが一度も増えないことしか証明しない。

---

## 2026-08-20 — S6b-c: P6 収集側 と、許可リストを強制に変えた

### 許可リストが「コメント」だった

`READ_KEYS` は「この reducer が見てよい jsonl キー」の allowlist として書かれていたが、**誰も強制していなかった**。P5/P3 の実装で `uuid` / `parentUuid` / `isApiErrorMessage` を読み始めたのに、3つとも allowlist に無いまま通っていた。

ソースを走査して `row['X']` の X が全て `READ_KEYS` にあることを検査するテストを追加。**強制されない allowlist は allowlist ではない。**

### `toolUseResult` の全面禁止は形が誤っていた

禁止の理由は `toolUseResult.interrupted` が死んでいる（2環境で 9,995 件・true 0）ことだった。しかし親オブジェクトごと禁じると、P6 が必要とする行数を**もっと悪い場所に探しに行かせる**。

- `FORBIDDEN_KEYS` を `toolUseResult` → **`toolUseResult.interrupted`** に絞った
- 代わりに `FORBIDDEN_SUBFIELDS`（`lines` / `originalFile` / `newString` / `oldString` / `content` / `stdout` / `stderr`）を明示
- **`toolUseResult` を開くのは `artifact.ts` 1ファイルだけ**にし、そこだけを検査対象にした

1ファイルに閉じ込めたのは検査を正確にするため。`scan.ts` は `message.content` を正当に読むので、両方を対象にすると `content` を許可せざるを得ず、危険な方も通ってしまう。「開くのが1ファイルであること」自体もテストしている。

### `newLines` は本文を読まずに取れる

`structuredPatch[].newLines` は**ログが既に計算済みの整数フィールド**。`lines`（差分本文）を一切開かずに取れる。create の場合は `file.numLines`。

### 実機（Python 独立測定と一致）

```
distinct な編集パス = 356   （Python 355、セッション進行分）
newLines 既知       = 225
newLines 不明       = 131
存在する            = 304
存在しない          =  52
束が付いている      = 356   （全件）
newLines 中央値     =  91   最大 2,326
```

### 「サイズ不明」を 0 にしない

131 パスは結果にサイズ情報が無い。**0 にすると空ファイルと区別がつかない。** v1 の重み `clip(1 + log10(newLines/50), 0.5, 3.0)` はどちらでも下限 0.5 に落ちるので、区別は数値の外（`newLinesKnown`）に置いた。何件が「サイズ不足」ではなく「データ不足」で下限に落ちたかが読める。

### 未実装（次）

SA の判定本体 — 右打ち切り（最終3日より前）/ 存在確認 / uptake / 重み / 束内クラスタリング。

---

## 2026-08-20 — S6b-d: P6 判定本体（確定成果物 SA）

### 実機

```
referenceTokens     = 1,488
consideredPaths     =   359
  最終3日以内の書込  =    91   （右打ち切り）
  もう存在しない     =    51
クラスタで統合       =    29
束あたり3件でクリップ =    33
SA 件数             =   155
totalWeight         = 130.32
uptake 1.0          =    76 / 155
サイズ不明で下限     =    29
```

v1 の欠測条件（SA 0件 または タスク束10未満）に対し **SA 155 / 束 417** なので、軸4 は分母側では欠測しない。

### v1 が撤廃した条件を、こちらも入れない

「**以後再編集なし**」は SA の条件から外す。v1 の実測では編集された 1,498 パスのうち2セッション以上にまたがって再編集されたのは 135 件（9.0%）。条件に入れると**手入れし続けているファイルほど分母から消える**という逆相関になる。再編集の有無は分子（深度）で扱う。

### 重みはディスク上のサイズを使わない

このマシンの編集パス 355 件のうち **180 件が作業ツリーの外**にある。存在確認だけを行い、**1バイトも読まない**。重みは書き込んだ行数（`structuredPatch[].newLines`）から。

### uptake は (a) だけ実装した — 過小に倒れる

| 条件 | 状態 |
|---|---|
| (a) 書込より後に参照された | ✅ 実装 |
| (b) 次窓でも存在し続けた | 初回窓では不能 |
| (c) p を引数にした実行が成功 | (a) より狭く、追加分しか生まない |

uptake は (a)∨(b)∨(c) なので、**(b)(c) のみを満たすパスが 1.0 ではなく 0.4 になる**。つまりこの実装は成果物スコアを**高くではなく低く**出す。倒れる向きを明示しておく。

### 参照の照合方向を逆にした

「全行を全パスで検索」は 30,000 行 × 356 パス。代わりに**各行からパス形のトークンを抽出して後で引く**形にした。トークン数はログが実際に言及した分に律速される（実測 1,488）。

正規化（小文字化 + `/` 統一）が要る。同じファイルが `c:\a\B.ts` と `c:/a/b.ts` で現れるため、**正規化しないと2つの綴りが偶然一致したときだけ「参照された」ことになる**。

### クラスタの代表は最重量にした

同一束・同一親ディレクトリ・同一拡張子・サイズ差2倍以内を1件に丸める。代表を「最初に書かれたもの」ではなく**最も重いもの**にしたのは、8つの兄弟パスに同じものを書く行為が1つの仕事であり、代表が最小のときだけ束の重みが不当に下がるため。

---

## 2026-08-20 — S9a: エラー署名の分類と窓内再発率

### 分類器の1回目は 63.1% を `other` に落としていた

「難しいデータ」ではなく「**読んでいないデータ**」だった。`other` バケツを開けたら原因が出た:

```
[Bash] 'Permission for this action was denied by the Claude Code aut...'
[Bash] 'Permission to use Bash with command cd /c/Users/katuy/...'
```

こちらの正規表現は `[Pp]ermission denied` を見ていた。**この環境の最頻の失敗を丸ごと取りこぼしていた。**

文言に合わせて直した結果:

| クラス | 件数 | 割合 |
|---|---|---|
| other | 103 | 32.5% |
| **permission_denied** | **88** | **27.8%** |
| no_such_file | 47 | 14.8% |
| edit_stale_read | 32 | 10.1% |
| traceback | 22 | 6.9% |
| timeout | 12 | 3.8% |
| syntax_error | 6 | 1.9% |
| edit_string_not_found | 4 | 1.3% |
| cmd_not_found | 3 | 0.9% |

**`other` 63% → 32.5%。** 一般化すると: **`other` が大きい分類器は、難しい問題ではなく開けていないバケツを見ている。**

### 懸念を実測したら外れていた

「`other:` を先頭60字のハッシュで切ると、埋め込まれたパスだけが違う同種の失敗が別署名になり r_in が過小に出る」と考えた。

**測った**: `other` の distinct 署名 70 件を、パスを伏せ字化してから数え直すと **69 件**。1件しか畳まれない。この環境では懸念は成立しない。

### r_in = 0.5142

```
errors = 317   distinctSignatures = 154   r_in = 1 - 154/317 = 0.5142
```

Python の粗い確認は 0.6625 だったが、**あちらは (tool, class) の2項、TS は v1 どおり (tool, class, target) の3項**。target が増えると署名が分かれるので r_in は下がる。定義の差であって不一致ではない。

### target 項が空回りしていた

最初の実装は target に**ツール名のハッシュ**を入れていた。ツールごとに定数なので、署名は実質 (tool, class) にしかならず、**違うファイルに対する失敗を区別できない**。v1 の定義（拡張子 / コマンド第1トークン）に直した。

### エラー0件の r_in は 1 ではなく 0

「失敗ゼロ」は「再発を測る対象が無い」であって「全部が再発」ではない。1 を返すと、**最も静かなログに最も大きい値**が付く。

### 文字クラスがまた潰れた

`/[\/]/` が `/[\/]/` になり、**バックスラッシュ分割が黙って消えていた**（Windows パスが1トークンになる）。今日2度目。テストに「バックスラッシュでも分割する」を明示で入れた。

---

## 2026-08-20 — S9b: 軸2（空振り率）— 初めて点が出た

```
生カウント   failures 322 / hookOriginated 0 / writeRepeats 143 /
             investigationRepeats 350 / timedOut 2 / largeOutput 1
重み付け後   failures 322 / writeRepeats 100.1 / investigationRepeats 70 /
             timedOut 2 / largeOutput 0.5
numerator = 494.60   bundles = 427   W = 1.1583  (W_ref 3.0)
r_in      = 0.5124
score     = 100 * (1 - 0.65*min(1, 1.1583/3.0) - 0.35*0.5124) = 57.0
```

### 計算していない項を4つとも名前で持つ

| 省いた項 | 効果の向き |
|---|---|
| `unused-success`（成功したが寄与しなかった呼び出し 0.5×） | **点が高く出る** |
| `verification-exclusion`（軸3が検証と数えた id を反復から除外） | **点が高く出る** |
| `user-script-decay`（自作スクリプトの Traceback を 0.3 に減衰） | **点が高く出る** |
| `winsorisation`（束あたり呼び出しの p90 で頭打ち） | **点が低く出る** |

**倒れる向きを両方書く。** 分子の正の項を落とせば W が小さくなり点は高く出る。winsorize を適用しなければ重い束が青天井に効き、点は低く出る。「未実装」とだけ書くと、どちらに外れているか読めない。

`unused-success` は proxy を作らなかった。任意の結果テキストを以後の全ログと突き合わせるのはパス照合とは別の問題で、**でっち上げた近似は誰も擁護できない数字を出す**。

### hookOriginated = 0（v1 の 29件は別環境）

hook 起因の失敗は分子・分母とも除外する規則だが、このマシンでは `(Pre|Post)ToolUse:` を含む tool_result が 17 件あり、**そのうち `is_error` が true のものは 0 件**だった。Python と TS で一致。

### winsorise の p90 がオフバイワンだった

`floor(0.9n)` は **0.9n が整数になる n で最大値を返す**。つまり cap を超える要素が構造的に 0 件になる。実データ（n=357）では index 321 が妥当な値だったので**もっともらしく通り**、n=10 のテストで初めて出た。

`ceil(0.9n) - 1`（nearest-rank）に修正。**大きい n でだけ正しく見える式は、正しい式ではない。**

### 束あたり呼び出し 357 と 分母 427 は別物

`winsorised.bundles` は**ツール呼び出しが1回以上あった束**（357）。分母は束数（427）。差の 70 束は「質問して答えて終わった」もので、**空振りゼロの仕事として正しく W を下げる**。

### 反復の判定は束スコープ

同じ Grep でも、別の依頼の中でなら別の調査。グローバルに数えると **1回目を2回目として課金**する。

---

## 2026-08-20 — S9c: 軸2をペイロードに載せた

```json
"wastedMotion": {
  "availability": "available",
  "lineStates": { "available": 17136, "not_applicable": 15346, "parse_failed": 0 },
  "score": 57.1,
  "confidenceInterval": null,
  "belowMinDenominator": true,
  "unavailableReasons": [],
  "detail": {
    "wastedTotal": 495.6, "bundleCount": 431,
    "wastedPerBundle": 1.1499, "repeatRate": 0.5139,
    "failures": 323, "writeRepeats": 143,
    "investigationRepeats": 350, "hookOriginatedExcluded": 0
  }
}
```

`lineStates` 合計 = 32,482 = `linesRead`（V-10 成立）。

### 可用性は「クラスタ数」ではなかった

こちらは全軸を「クラスタ 11 < 最小 20 だから not_applicable」で埋めていた。**v1 の軸2の条件は「フィルタ後の tool_use が50未満で not_applicable」**で、このマシンは 8,502 なので **available**。

最小分母（クラスタ20 / 分母200 / 分子5）がゲートするのは **Δ を「改善／悪化」と呼んでよいか**であって、率が存在するかではない。混同した結果、**8,502 回ツールを呼んだ環境を「測るものが無い」と報告していた**。

分けた:

| フィールド | 意味 |
|---|---|
| `availability` | 軸ごとの条件（軸2は tool_use ≥ 50） |
| `belowMinDenominator` | 最小分母3条件。Δ と CI をゲート |
| `confidenceInterval` | 最小分母を割っているので `null` |

### W を numerator/denominator という名前で載せない

軸2の率は「タスク束あたりの空振り手数」で、**設計上 1 を超える**（実測 1.15）。

`numerator` / `denominator` という名前で載せると **V-5（分子 > 分母を拒否）に引っかかる**。かといって例外扱いにすると、**107.1% が通り抜けた穴をルールに開ける**ことになる。

`detail` に `wastedTotal` / `bundleCount` / `wastedPerBundle` という名前で持たせた。再計算はできて、比率でないものを比率の名前で呼ばない。

---

## 2026-08-23 — countBasis と V-25（NiKo Q-6 確定への追随）

NiKo が 8/21 に `countBasis` を必須化した（`213a532`）。動機は仕様自身の傷:

> `permission-rule` の件数が仕様の4箇所で 40 / 41 / 44 / 45 と食い違ったのは、4つが違う量だったからではなく、**それぞれが違う数え方で、どれも数え方を書かなかったから**（同じ量が **39〜52 = 1.33倍**動く）

率には `denominatorMeaning` があったが、**件数には数え方を書く場所が無かった**。

### 実装の実態が既定と3点ずれていた

`basis` に既定値をそのまま書けば通ったが、**それは嘘になる**ので実測してから書いた。

| 項目 | 仕様の既定 | 本実装の実態 | 根拠 |
|---|---|---|---|
| `scope` | `all` | `all` | main + subagents を走査 |
| `period` | `window` | **`allTime`** | `windowDays` はペイロードに載るだけで**フィルタに使っていない**。走査は刈られ残った全件 |
| `unit` | `toolUseId` | **`row`** | `denialRows` は行で加算 |
| `excludes` | `["infrastructure-error"]` | **`[]`** | `Tool permission stream closed` が実データに **11 件**あるが除外していない |

**3つの差は全部この実装の欠落**で、`countBasis` はそれを隠さず表に出す。既定値を書き写していたら、`period: "window"` と称して窓を適用しないという、**この項目が防ぐために作られた欠陥そのもの**になっていた。

### `excludes: []` は「除外なし」であって欠落ではない

空配列を V-25 が受理することをテストで固定した。除外しなかったことは提出物についての事実であって、提出物の中の抜けではない。

### 陽性対照

V-25 を `add()` で握りつぶすと拒否テスト5件だけが落ち、`accepts an empty exclusion list`（不在を主張するテスト）は通り続ける。331件中326件は無傷。

### 残っている追随

NiKo が定義した **V-17〜V-24 / W-8** は未実装。全て `composite` ブロック（総合点）に対する検査で、そのブロック自体がまだ無い。V-23（`belowMinDenominator` の軸があるのに significant）と V-24（`omittedTerms` があるのに `leans` が null）は、**こちらが実装で分けた区別と書いた慣習が仕様に昇格したもの**。

---

## 2026-08-23 — composite（総合点）と V-17〜V-24 / W-8

NiKo が 8/20 に §12 で合成式を確定（`2956466`）。重み表はあったが**それを消費する式が無く**、S12 が唯一そこで止まっていた。

### 実機

```json
"composite": {
  "score": null, "tier": null,
  "axesUsed": ["wastedMotion"], "nominalWeightSum": 17.5,
  "effectiveWeights": {},
  "excluded": ["firstPassLanding","selfVerification","artifactUptake",
               "environmentMetabolism","recurrencePrevention"],
  "suppressedReason": "too-many-missing-axes",
  "leans": "mixed",
  "outcomeScore": null, "designScore": null
}
```

**総合点が出ないのが第1弾の正常系**（仕様 §12.3 が明記）。6軸中5軸が未実装なので欠測上限2を超える。

### 欠測は0点でも最良値でもない（P-A）

v1 は前者だけを禁じていた。後者を禁じないと**測れない環境ほど高く出る**形が残り、これは本製品が最初に否定したもの（設定を凝ると点が上がる装置）と同型になる。テストで両方向を固定した。

### `leans` は「優勢な向き」ではなく「両方あれば mixed」

軸2は正の分子項を3つ（高く出る）と winsorisation（低く出る）を同時に落としている。重みで多数決すれば `high` になるが、仕様は名指しでこのケースを `mixed` と定めている。

> **どちらとも言えないことを言えないと書く**のが正しい状態で、精度が上がったふりをしない

実機の `leans` は `mixed`。

### `suppressedReason` の3語は実装側の語彙が採用された

仕様の初稿は `parse_rate` / `roots_incomplete` / `active_days` を新設していたが、**AB-88 で指摘したのと同じ形**（同じものを2文書が別名で呼び、受信側が黙って比較に失敗する）なので、`src/score/gate.ts` の `GateReason` の実文字列に合わせられた。新設は `too-many-missing-axes` の1語だけ。

### 向きの無い項を作れなくした

`OMITTED_TERM_LEANINGS` を `Record<OmittedTermName, 'high'|'low'>` で型付けした。`OMITTED_TERMS` に項を足して向きを書き忘れると**コンパイルが通らない**。向きの無い項は V-24 が拒否する対象で、ペイロードで捕まえるよりコンパイルで捕まえるほうが安い。

### V-21 / V-23 は実装しない

どちらも `comparison` ブロックを検査するが、そのブロックには前窓が要る。**書き出す仕組み（P4）がまだ無いので、常に通るルールはカバレッジに見えるだけ**。`NOT_IMPLEMENTED_HERE` に名前で残した。

### 陽性対照

新6ルールを `add()` で握りつぶすと拒否テスト10件だけが落ち、受理系4件と W-8 は通り続ける。368件中358件は無傷。

---

## 2026-08-23 — クラスタの畳み込み（P4 設計レビューの指摘 #3 を検証）

P4 の設計ワークフローが「`counts.sessionIds` は行の `sessionId` から作っており、v2:962 の `sub は親 session_id に畳む` に反する」と指摘した。**クラスタ数は最小分母（20）の判定に直結し、CI の可否を決める。**

### 実測: 指摘はこのデータでは誤りだった

```
行の sessionId (distinct) = 12      ファイル位置由来の親 id = 12
  うち main 行由来        = 12      sub にしか無い id       = 0
  行 id ⊆ 親 id           = True
```

サブエージェント行は**すでに親の sessionId を持っており**、新しい id を持ち込んでいない。現行実装の数え方は、このマシンでは仕様の畳み込みと一致する。

### ただし一致は偶然であって保証ではない

サブエージェントのトランスクリプトが自分の id を持つ環境では、**クラスタ数が黙って増える**。増えた先が 20 を超えれば `belowMinDenominator` が false になり、出るはずのない CI が出る。

数字は変えず、**根拠を偶然から構造に変えた**:

- クラスタはファイルの位置から畳む（`file.sessionId`）
- 行の `sessionId` はそれと突き合わせ、食い違いを `sessionIdMismatchRows` として数える

### 検出器の陽性対照を2段で取った

**測定器側** — 一時ツリーに「sub が独自 id を持つ」形を作ると (行 2, 親 1) と差が出る。同じ形で親 id を持たせると (1, 1)。指摘を「誤り」と判定する前に、**差を検出できることを先に示した**。

**実装側** — `scan.test.ts` のコーパスは行が `s1`・ファイルが uuid という乖離ケースなので、`sessionIdMismatchRows > 0` をテストで固定した。実機の 0 は、**発火できることが示された検出器の 0**。

### 副産物

このコーパスは今まで「行の id を集める」実装に合わせて `['s1']` を期待していた。畳み込みに変えたことで**期待値がファイル名側に移り**、コーパス自体が乖離の実例として機能するようになった。

---

## 2026-08-23 — P4 基盤（状態ディレクトリ・MAC・鍵）

P4 の設計を9エージェントのワークフローで固めた（読解4 / 設計1 / 敵対レビュー3 / 統合1）。本コミットは §1（配置）・§5.3（MAC）・§5.4（鍵）の実装。

### ディレクトリは無防備な状態で存在できない

「mkdir してからガードを書く」では、その間に落ちると**MAC を含むディレクトリがガード無しで残る**。staging 名で組み立て、`.gitignore` を中に入れてから `rename` する。rename は NTFS でも POSIX でも原子的で、**最終名で見えた時点でガードは既に中にある**。

`.gitignore` は毎回読み直して、`*` の行が無ければ書き直す。守っている相手は「数か月後にエディタやバックアップツールや `git clean` がドットファイルを消す」で、それは何も告げない。

### git 追跡の拒否条件を狭めた

設計初稿は「`.git` が STATE_DIR かその上位にあれば拒否」だった。**ホームディレクトリを git 管理している人は `~/.git` を持つ**ので、この条件では毎回・永久に拒否され、このモジュールが守るはずのものを失わせる。

リポジトリの中にいることはリスクではなく、**コミットされていることがリスク**。`git ls-files --error-unmatch` が 0 で返るときだけ拒否し、`--state-dir` という逃げ道を示す。

### 鍵は毎回変えられない — 変えると満点になる

軸6の窓跨ぎ再発は MAC 集合の交差。鍵が変われば**交差は空**になり、再発率 0 ＝「同じ失敗を二度としない環境」として満点が出る。v1 §16 が挙げた攻撃を、事故で踏む形。

一方で永続鍵は永続的な秘密。**解決はローテーション計画ではない**: ローテーションには平文が要り、平文は設計上捨てているので、新鍵は旧集合を引き継げない。誰も払わないコストなので、鍵は実質 write-once。実際に効く緩和は (a) 鍵をストアの外に置く (b) 集合の保持期間を切る、の2つだけ。

### 交差の可否は epoch ではなく fingerprint で見る

鍵ファイルと `key.epoch` を一緒に消してスナップショットだけ残ると、新しい鍵が **epoch 1 を名乗る**。epoch で判定すると2つの別鍵が同じラベルを共有し、交差が空になって満点が出る。`fingerprint` 不一致で拒否し、**epoch が一致して fingerprint が違う場合を `key-identity-conflict` として別名で報告**する（何も異常に見えないぶん、こちらのほうが悪い）。

### ファイル名の前方一致が罠

`~/.agent-eval-key` は `~/.agent-eval` を**前方一致で含む**。`startsWith` で「ストアの中か」を判定するコードは、鍵をストアの中と誤判定する。バックアップ除外なら鍵を巻き込み、クリーンアップなら**比較の唯一の手段である鍵だけを消してスナップショットを残す**。テストで固定した。

### 設計書の算術が1件誤っていた

「`bytesRead` は 1e9 に達し、1e9 × 1e4 は 2^53 を超える」と書かれていたが、**1e13 < 9.007e15** で超えない。ガード自体は残したが、根拠を実際の閾値（約 9.007e11）と「この尺度を取るフィールドは全て率か 0-100 の点なので、これを超える値は count が誤ったコンストラクタに来た印」に書き換えた。**テストを書いたら落ちて分かった。**

---

## 2026-08-23 — P4 §8.1 / §8.2（失うと戻らない2件）

設計書 §8 は上流の変更を**不可逆性の順**に並べ、「戻らないのは最初の2つだけ」と書いている。本コミットはその2つ。

### §8.2 セッション単位の集計

ブートストラップはセッションを復元抽出し、**引いたメンバーから比推定量を計算し直す**。集計値は1つしか無いので再抽出できない。`perSession` にセッションごとの分子・分母を持たせた。

これが無いと **Δ の信頼区間は永久に計算できない**（元の生ログは刈られる）。

親への畳み込みはファイル位置から。サブエージェントを別クラスタに数えるとクラスタ数が水増しされ、20 を超えれば**出るはずのない CI が出る**。

**検算をテストで固定した**: セッション別の合計が `taskBundles` / `wasted.failures` / `writeRepeats` / `linesRead` と一致すること。一致しなければどちらかが別のものを測っている。

### §8.1 署名集合を MAC で出す

**平文タプルは `scan()` のスタックフレームから出さない。** `repeatRate` はフレーム内のタプルから計算し、外に出るのは集約値と MAC だけ。

`sign` は注入。鍵が reducer に入らず、reducer が平文を返さない。

**`signaturesSigned` で「署名器が無かった」と「失敗が無かった」を分ける。** どちらも空配列で、意味は正反対。

### 平文を守っていた根拠が、ファイルを書いた瞬間に消える

`hashTarget`（無塩 sha256 8桁）を削除した。コメントの弁明は「値はこのプロセスから出ない」で、**それはスナップショットを書くまでは真だった**。拡張子とコマンド名の空間に対する 8 桁は列挙で戻せる。署名タプル全体に対する MAC が target も覆うので、置き換えで情報は減らない。

### エスケープが4度目に潰れた

区切り文字を書いたつもりが、**リテラルの NUL バイトがソースに書き込まれていた**（grep が「binary file」と言って発覚）。`String.fromCharCode(0)` の名前付き定数 `SIG_SEPARATOR` にした。区切りを明示的に持つのは、`("a","b:c","")` と `("a:b","c","")` が同じ署名に潰れないため。

### 副作用は既定でオフ

`--store` を付けたときだけストアと鍵を作る。**印字するだけの走査がユーザーのマシンに副作用を持つべきではない。** `--state-dir` を `--store` 無しで渡すと黙って無視されるので、エラーにした。

実機検証は home を隔離して実行し、**実ホームには鍵もストアも作っていない**。

---

## 2026-08-23 — P4 正準形とハッシュ

チェーンは「これは変わっていない」と言うためにある。**1つの値に2通りのバイト列があると、起きていない変化を報告する**。

### `JSON.stringify` は使えない

- オブジェクトのキーが**挿入順**。同じレコードを2つの経路で作ると別のハッシュになる
- `-0` と `1e+21` — 1つの値に2つの綴りがある

### 数値は安全整数のみ

浮動小数の書式は JSON のうち**処理系で差が出る部分**で、印字器に依存するハッシュはチェーンではない。分数は `e4()` で 10,000 倍してから来る。**型が `E4` と言い、シリアライザが実行時にそれを強制する**（片方だけでは足りない）。

### 正規表現をやめた — 誤っていた

高速パス判定を `/["\]|[ -]/` と書いていた。**バックスラッシュが閉じ角括弧をエスケープしてしまい**、最初の文字クラスは `"` `]` `|` `[` を持ち、バックスラッシュには一切マッチしなかった。

結果: **バックスラッシュを含む文字列が高速パスに落ち、エスケープされずに出力される**。不正な JSON で、存在しない値のハッシュが出る。

**読んでは見つからなかった。** ソースから該当行を取り出して評価し、ケースごとに実測して分かった。文字クラスは、まさに「読むと分からない」場所。

コード単位の走査に置き換えた。エスケープの解釈が入る余地が無い。

### 陽性対照

`needsEscape` からバックスラッシュ判定だけを外す（旧バグの再現）と、**2件が落ちる**: 「バックスラッシュをエスケープする」と「JSON.parse で往復する」。後者があるのは、出力が入力と違うだけでなく**読めることまで**必要だから。

### ドメイン分離

`agent-eval/snapshot/1` と `agent-eval/sidecar/1` を前置する。プロジェクト id・エラー要約・入力キーはどれも**人が形を左右できるテキスト**に対する sha256 なので、前置が無いと1つの空間で作った値が別の空間で通ってしまう。

### 今日5回目のエスケープ事故

正規表現の文字範囲がリテラルの制御文字としてソースに書き込まれていた。今日これで **NUL 混入・引用符・改行・文字クラス** と5件。**書いた文字列が意図どおりかは、書いた側からは見えない。** 実測（od / grep -a / 評価してテスト）でしか分からない。

### 追記: 制御文字を構造的に止めた

正準形のバグを直した後、**全ソースを走査したら他に3ファイル**で生の制御文字が見つかった（`errorClass.ts` 2件 / `wasted.ts` 2件 / `mac.test.ts` 4件）。いずれも区切り文字としての NUL で、**機能的には正しく動いていた**。

問題は動作ではない:

- レビューで**見えない**
- grep がファイルを binary 扱いにする（`hashTarget` の未使用検出でこれに引っかかった）
- 空白を正規化するツールが、誰にも見える行を触らずに挙動を変える

全て `String.fromCharCode(0)` の名前付き定数にした。

**`test/sourceHygiene.test.ts` を追加**して、`src/` と `test/` の全 `.ts` に生の制御バイト（タブ・改行・復帰を除く）が無いことを検査する。

1日に5回起きた。**習慣でしかないルールは、注意が他所にある日に破られる。そしてそれは、まさに効いてほしい日である。** 走査範囲（ファイル数）も assert しているので、0 が「走査していない 0」ではないことが分かる。陽性対照も付けた。

---

## 2026-08-23 — P4 本体（レコード・ハッシュ鎖・書き込み）

実データで seq 1 を書けた。6,267 バイト / セッション12 / 署名170（エラー366）/ 恒等式5件成立 / 未構築3ブロックを実名で記録。ユーザー名・プロジェクト名・コマンド・パスの混入なし。

### 「今日書かないと戻らない」が形を決めている

- **記録しなかった値は消える** → 全11軸を窓1から載せる（大半は `not-implemented`）。後から現れた軸は、初回窓が何とも比較できない
- **誤って記録した値はファイルが残る限り誤り** → 欠落ブロックは欠落と言い、未実装軸は測定ゼロに読めず、**genesis と履歴消失は別コンストラクタ**

### 実際にスコアを動かす定数を指紋にした（設計レビュー指摘 #2）

`VERSION` は `'0.0.0'`、`parserVersion` は `'1'` の**リテラル型で、定義変更を記録できない**。実際に軸2の数字を動かすのは `W_REF` で、**どこにも記録されていなかった**。

`formulaFingerprint` に入れた。変えれば指紋が変わり、比較規則が**名指しで拒否**する。定義の変更を環境の変化として報告しない。

### 初回実行が拒否で見つけた3件

**1. ペイロードを snapshot の正準形でハッシュしていた。** ペイロードは率を持ち、正準形は整数のみ。当然落ちたが、**エラーがパスを言わなかった**。パスを出すようにしたら、残り2件が1手ずつで見つかった。ペイロードは**出荷するバイト列**で同定する。

**2. `formulaFingerprint` が `weightInvestigationRepeat: 0.2` で落ちた。** 定数は全部分数で、**まさに E4 が存在する理由**。スケールし、接尾辞で尺度を読み手に運ぶ。

**3. `day-nesting` を等式で実装していた。** 実体は包含関係（人間発話日 ⊆ user行日）。このマシンで通ったのは**たまたま両方 10 だったから**で、両者が違う通常の環境では**正しいデータを拒否**していた。恒等式に関係（`equal` / `atMost`）を持たせた。

### セッションは MAC 順に並べる

平文 id 順に並べると、**id を載せないためのファイルに id の相対順序が漏れる**。

### 書き込み

sidecar → body → HEAD の順で rename。途中で落ちても、**未コミットの sidecar（後で掃除される）が残るだけで、存在しないファイルを指す body は生まれない**。

ロックは**経過時間ではなく pid の生存**で判定する。壁時計で奪うと、閾値を超える時計の進みが**生きているロックを奪い、ロックが防ぐはずのフォークを作る**。

`writeSnapshot` は呼び出し元に例外を投げない。**ストアが書けない日でもペイロードは出る**必要がある。

---

## 2026-08-23 — チェーン検証と CLI 表示

### 保存されたハッシュを信じない

全ハッシュを内容から再計算する。**信じた保存済みハッシュは、書き換えた本文に合わせて書き換えられるハッシュ**であり、構造全体が飾りになる。

実データで2窓を書き、リンクを確認:

```
1回目: seq 1  prev={"kind":"genesis"}
2回目: seq 2  prev={"hash":"sha256:4e091ac0f1ad..."}
chain: length 2, continuous true, consecutiveTail 2, breaks []
```

**陽性対照**: 1つ目を書き換えると `hash-mismatch` が seq 1 と seq 2 の**両方**に出て、`consecutiveTail` が 2 → 0 になる。**過去を書き換えると、書き換えた行だけでなく履歴全体を失う。**

保存済みハッシュも一緒に書き換える（実際に試みられる偽造）ケースは、**子が捕まえる**。そこがチェーンの性質の在り処。

### 検出するもの

連番の欠落 / フォーク / 読めない本体 / sidecar の削除・改変 / 鍵の fingerprint 変化 / 時計の逆行。

**時計は記録するだけで補正しない。** 黙って補正するのが「もっともらしい誤った数字」の作り方。

### 何を establish しないかを名前で持つ

`NOT_ESTABLISHED` は免責文ではない。鍵はローカル・ハッシュ関数は公開なので、**ストアと鍵に書き込める者は自己整合的な偽の履歴を作れる**。

CLI は "verified" ではなく **"self-consistent"** と書く。テストは主張ではなく**実演**する — ゼロから作り直したチェーンは、育ったチェーンと**まったく同じくらい clean に検証される**。

### 走査範囲を verdict に持たせる

**走査していない 0 は、綺麗なチェーンと見分けがつかない。**

### スナップショット行は毎回出す

```
snapshot   wrote #2 (15f9b5a250bd), chain 2 self-consistent — 3 block(s) absent
snapshot   not written (--store to keep history)
snapshot   REFUSED: locked — another run holds the lock
```

**黙って書けなかったスナップショットは、1窓あとに「比較できない」という形で見つかる。** パスは載せない（OS ユーザー名を含み、この出力は issue に貼られる）。
