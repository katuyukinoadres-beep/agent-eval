# 第三者の著作物について

このディレクトリの中身は **本リポジトリの MIT ライセンスの対象ではありません。**

| ファイル | 著者 | 種別 | 行数 |
|---|---|---|---|
| `2026-08-18_agent_eval_axes_v1.md` | NiKo | 測定軸の仕様 | 1,123 |
| `2026-08-19_agent_eval_axes_v2.md` | NiKo | 同上（改訂） | 1,507 |
| `2026-08-19_submission_payload_spec_v1.md` | NiKo | 提出ペイロード仕様 | 419 |
| `measure_agent_env.py` | NiKo | 参照実装 | 231 |

実装（`src/`）は本リポジトリの著者のもので MIT。**仕様と参照実装は別の著者のもの**で、再配布の条件は合意されていない。

## 現在の状態（2026-08-30 時点）

この文書は以前「**公開前に**許諾を済ませること」と書いていた。**その順序は守られていない。** 実際には次の順で起きた。

| 日付 | 出来事 |
|---|---|
| 2026-08-24 | 著者（NiKo）へ許諾を依頼（Agent Bridge AB-92） |
| 2026-08-25 | **返答を待たずに、オーナー判断でリポジトリを public 化** |
| 2026-08-30 | AB-92 は未回答のまま |

つまり **これら 4 ファイルは、許諾が確認されないまま公開配布されている。** 事後にでも解決すべき状態であり、解決済みではない。

## 判断材料（git と GitHub で実測）

- `docs/spec/` に触れた 7 コミットは、**author も committer もリポジトリのオーナー本人**
- 仕様の本拠 `katsu-agents` の GitHub owner id は本リポジトリと**同一アカウント**

したがって**法的な第三者は存在しない可能性が高い**。この NOTICE は、オーナー自身のエージェントが書いた成果物について、オーナー自身が置いた注意書きである。ただし**「可能性が高い」は確認ではない**ので、記録は残す。

## 残っていること

1. AB-92 の回答を得て、この節を「許諾済み・条件は◯◯」に置き換える
2. 不可だった場合、このディレクトリを外す。**`git rm` では消えない** — blob は 7 コミットに残り、`refs/pull/*` 37 本は push で削除できないため GitHub Support への依頼が要る
3. npm パッケージには **含めていない**（`package.json` の `files` は `dist` / `README.md` / `LICENSE` / `docs/PRIVACY.md` / `docs/INSTALL.md` / `docs/GUIDE.md` のみ。`npm pack` 実測 52 ファイル中 0 件）

ライセンスの無い第三者著作物を再配布すると、受け取った側にも利用の権利が渡らない。**この 4 ファイルを引用・再配布する場合は、上記が未解決であることを前提にすること。**
